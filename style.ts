import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    fontFamily: 'Poppins-Regular',
  },
  text: {
    fontSize: 16,
    color: '#000000',
    fontWeight: 'bold',
  },
});
